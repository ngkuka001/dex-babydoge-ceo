import { Typography } from 'antd';

const WinPage = () => {
  return (
    <section>
      <div
        style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100%', minHeight: '100vh' }}
      >
        <Typography.Title style={{ color: '#34C5FE', fontSize: '60px' }}>Coming Soon</Typography.Title>
      </div>
    </section>
  );
};

export default WinPage;
